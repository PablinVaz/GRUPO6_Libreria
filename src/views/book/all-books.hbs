<h1 class="text-center">Catalogo de libros</h1>
<div class="row">
    
    {{#each books}}
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-items-center">{{title}} <a href="/book/edit/{{_id}}"><i class="fa fa-edit"></i></a></h4>
                    <p>{{publisher}}</p>
                    <p>{{price}} &euro;</p>
                    <p>{{stock}}</p>
                    <form action="/book/delete/{{_id}}" method="POST">
                        <button type="submit" class="btn btn-danger btn-block btn-sm">Borrar</button>
                    </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>